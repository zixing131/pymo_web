{
  "name": "pymo-web",
  "version": "1.0.0",
  "description": "PyMO Visual Novel Engine for Web",
  "scripts": {
    "build": "babel src -d dist --presets=@babel/preset-env",
    "build:kaios": "babel src -d dist-kaios --config-file ./babel.kaios.json"
  },
  "devDependencies": {
    "@babel/cli": "^7.28.3",
    "@babel/core": "^7.28.5",
    "@babel/preset-env": "^7.28.5"
  },
  "browserslist": [
    "Firefox >= 48"
  ]
}
